<?xml version="1.0" encoding="utf-8"?>

<ContentPage
    x:Class="KiirLink.Pages.CreateAccountPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:l="clr-namespace:KiirLink.Extensions"
    xmlns:viewModels="clr-namespace:KiirLink.ViewModels"
    x:DataType="viewModels:CreateAccountViewModel"
    BackgroundColor="{DynamicResource AppBackground}"
    Title="{l:Translate CreateAccount}">

    <Grid BackgroundColor="{DynamicResource AppBackground}" RowDefinitions="*,Auto">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout
                Padding="{StaticResource PagePadding}"
                Spacing="18"
                VerticalOptions="Center">

                <VerticalStackLayout Spacing="10">
                    <Image
                        HeightRequest="72"
                        HorizontalOptions="Center"
                        Source="kiirlink_logo.png"
                        WidthRequest="72"
                        SemanticProperties.Description="KiirLink logo" />
                    <Label
                        HorizontalTextAlignment="Center"
                        FontFamily="RadioCanadaBigSemiBold"
                        FontSize="24"
                        Text="{l:Translate CreateAccount}" />
                    <Label
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource MutedLabel}"
                        Text="{l:Translate CreateAccountSubtitle}" />
                </VerticalStackLayout>

                <Border Padding="{StaticResource CardPadding}" Style="{StaticResource CardBorder}">
                    <VerticalStackLayout Spacing="12">
                        <VerticalStackLayout Spacing="6">
                            <Label FontSize="11" Text="{l:Translate Email}" />
                            <Entry Keyboard="Email" Placeholder="you@example.com" ReturnType="Next"
                                   Text="{Binding Email}" SemanticProperties.Description="Email address" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label FontSize="11" Text="{l:Translate Password}" />
                            <Entry IsPassword="True" Placeholder="{l:Translate Password}" ReturnType="Next"
                                   Text="{Binding Password}" SemanticProperties.Description="Password" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label FontSize="11" Text="{l:Translate ConfirmPassword}" />
                            <Entry IsPassword="True" Placeholder="{l:Translate RepeatPassword}" ReturnType="Done"
                                   Text="{Binding ConfirmPassword}" SemanticProperties.Description="Confirm password" />
                        </VerticalStackLayout>

                        <Button
                            Command="{Binding CreateAccountCommand}"
                            IsEnabled="{Binding CanInteract}"
                            Style="{StaticResource PrimaryButton}"
                            Text="{l:Translate CreateAccountAction}" />

                        <Button
                            Command="{Binding BackCommand}"
                            Style="{StaticResource OutlineButton}"
                            Text="{l:Translate BackToSignIn}" />
                    </VerticalStackLayout>
                </Border>

                <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                                   SemanticProperties.Description="Creating account" />
                <Border IsVisible="{Binding IsOffline}" Style="{StaticResource OfflineBanner}">
                    <Label HorizontalTextAlignment="Center" Text="{l:Translate NoInternetConnection}"
                           TextColor="{DynamicResource AppDangerText}" />
                </Border>
            </VerticalStackLayout>
        </ScrollView>

        <Label
            Grid.Row="1"
            Margin="24,0,24,18"
            HorizontalTextAlignment="Center"
            Style="{StaticResource MutedLabel}"
            Text="{l:Translate ProtectedWorkspace}" />
    </Grid>
</ContentPage>
